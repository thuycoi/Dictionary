<%= form_for(gword) do |f| %>
  <% if gword.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gword.errors.count, "error") %> prohibited this gword from being saved:</h2>

      <ul>
      <% gword.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Wort %>
    <%= f.text_field :entry %>
  </div>

  <div class="actions">
    <%= f.submit :Fertig %>
  </div>
<% end %>
